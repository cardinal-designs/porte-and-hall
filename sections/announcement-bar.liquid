<div class="announcement-bar">
  {% if section.settings.link != blank %}
    <a href="{{ section.settings.link }}" class="announcement-bar__item">
  {% endif %}
    <div class="page-width {% if section.settings.show_popup == true %}cursor_pointer {% endif %}" {% if section.settings.show_popup == true %} onClick="Privy('show', 2149710);" {% endif %}>
      {{ section.settings.text }}
    </div>
  {% if section.settings.link != blank %}
    </a>
  {% endif %}
</div>

<style>
  .announcement-bar {
    color: {{ section.settings.text_color }};
    background-color: {{ section.settings.background_color }};
  }

  .cursor_pointer {
    cursor: pointer;    
  }

  .announcement-bar a {
    color: {{ section.settings.text_color }};
  }
  
  :root {
    --mobile-header-height: 96px;
  }
</style>

{% comment %}
{% javascript %}
  const announcementBarSlider = new Swiper('.announcement-bar', {
    slidesPerView: 1,
    loop: true,
    spaceBetween: 20
  });
{% endjavascript %}
{% endcomment %}
 

{% schema %}
{
  "name": "Announcement Bar",
  "settings": [
    {
      "type": "text",
      "id": "text",
      "default": "Welcome to our store",
      "label": "Text"
    },
    {
      "type": "url",
      "id": "link",
      "label": "Link"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background Color",
      "default": "#546B82"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text Color",
      "default": "#ffffff"
    },
    {
      "type": "checkbox",
      "id": "show_popup",
      "label": "Show Privy Privy",
      "default": true
    }
  ]
}
{% endschema %}
