{% comment %}
    Renders an article card for a given blog.

    Accepts:
    - blog: {Object} Blog object
    - article: {Object} Article object

    Usage:
    {% render 'article-card' blog: blog, article: article %}
{% endcomment %}

<div class="article-card">
  <a href="{{ article.url }}">
    <div class="article-card__image">
      {% render 'lazy-image' image: article.image, fade: true, classes: 'img-fill' %}
    </div>
  </a>
  <div class="article-card__container">
    <p class="article-card__date">{{ article.published_at | time_tag: format: 'date' }}</p>
    <h6 class="article-card__title">{{ article.title }}</h6>
  </div>
</div>