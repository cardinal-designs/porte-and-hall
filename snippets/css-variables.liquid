{% style %}
    /* FONTS */

    @font-face {
      font-family: handwriting;
      src: url('{{ "LaBelleAurore-Regular.ttf" | asset_url }}') format('truetype');
      font-weight: 400;
      font-style: normal;
      font-display: swap;
    }

    {%- comment -%}
      src order must be woff2 → woff → ttf. Browsers pick the first supported
      format; listing ttf first forced ~100KB TTF downloads despite woff2 preloads
      and starved mobile LCP (hero "Summer, Come On In" uses louize 500).
    {%- endcomment -%}
    @font-face {
      font-family: louize;
      src: url('{{ "LouizeDisplayTrial-Regular.woff" | asset_url }}') format('woff'),
        url('{{ "LouizeDisplayTrial-Regular.ttf" | asset_url }}') format('truetype');
      font-weight: 400;
      font-style: normal;
      font-display: swap;
    }

    @font-face {
      font-family: louize;
      src: url('{{ "LouizeDisplayTrial-Medium.woff2" | asset_url }}') format('woff2'),
        url('{{ "LouizeDisplayTrial-Medium.woff" | asset_url }}') format('woff'),
        url('{{ "LouizeDisplayTrial-Medium.ttf" | asset_url }}') format('truetype');
      font-weight: 500;
      font-style: normal;
      font-display: swap;
    }

    @font-face {
      font-family: brandon;
      src: url('{{ "BrandonGrotesque-Light.woff2" | asset_url }}') format('woff2'),
        url('{{ "BrandonGrotesque-Light.woff" | asset_url }}') format('woff');
      font-weight: 300;
      font-style: normal;
      font-display: swap;
    }

    @font-face {
      font-family: brandon;
      src: url('{{ "BrandonGrotesque-Regular.woff2" | asset_url }}') format('woff2'),
        url('{{ "BrandonGrotesque-Regular.woff" | asset_url }}') format('woff'),
        url('{{ "BrandonGrotesque-Regular.ttf" | asset_url }}') format('truetype');
      font-weight: 400;
      font-style: normal;
      font-display: swap;
    }

    @font-face {
      font-family: brandon;
      src: url('{{ "BrandonGrotesque-RegularItalic.woff2" | asset_url }}') format('woff2'),
        url('{{ "BrandonGrotesque-RegularItalic.woff" | asset_url }}') format('woff'),
        url('{{ "BrandonGrotesque-RegularItalic.ttf" | asset_url }}') format('truetype');
      font-weight: 400;
      font-style: italic;
      font-display: swap;
    }

    @font-face {
      font-family: brandon;
      src: url('{{ "BrandonGrotesque-Medium.woff2" | asset_url }}') format('woff2'),
        url('{{ "BrandonGrotesque-Medium.woff" | asset_url }}') format('woff'),
        url('{{ "BrandonGrotesque-Medium.ttf" | asset_url }}') format('truetype');
      font-weight: 500;
      font-style: normal;
      font-display: swap;
    }

    @font-face {
      font-family: brandon;
      src: url('{{ "BrandonGrotesque-Bold.woff2" | asset_url }}') format('woff2'),
        url('{{ "BrandonGrotesque-Bold.woff" | asset_url }}') format('woff'),
        url('{{ "BrandonGrotesque-Bold.ttf" | asset_url }}') format('truetype');
      font-weight: 600;
      font-style: normal;
      font-display: swap;
    }

      :root {
        --header-height: 119px;
        --mobile-header-height: 68px;
        --announcement-bar-height: 0;

        /* Brand Colors */
        --gold: #896A28;
        --gold-secondary: #896A28;
        --gold-secondary--dark: #896A28;

        --blue: #546B82;
        --blue-secondary: #30383D;

        --beige: #D9D3C4;
        --beige-secondary: #F4F2EE;

        --off-white: #EEEFEF;
        --white: #FFFFFF;
        --gray: #999999;
        --light-gray: #F5F5F5;

        /* Status Colors */
        --disabled: #999999;
        --focus: #F4F2EE;
        --error: #FF0000;
      }

      *,
      *::before,
      *::after {
        box-sizing: inherit;
      }

      html {
        box-sizing: border-box;
        height: 100%;
      }

      body {
        display: grid;
        grid-template-rows: auto auto 1fr auto;
        grid-template-columns: 100%;
        min-height: 100%;
        margin: 0;
        font-family: brandon, sans-serif;
        color: {{ settings.text_color }};
        background: {{ settings.background_color }};
      }

      a {
        text-decoration: none;
        color: inherit;
      }

      button {
        margin: 0;
        padding: 0;
        border: 0;
        background: none;
        -webkit-appearance: none;
        appearance: none;
        cursor: pointer;
        color: {{ settings.text_color }};
      }

      .blue {
        color: var(--blue);
      }

      .blue-secondary {
        color: var(--blue-secondary);
      }

      .gold {
        color: var(--gold);
      }

      .gold-secondary {
        color: var(--gold-secondary);
      }

      .gold-secondary--dark {
        color: var(--gold-secondary--dark );
      }

      .gold--dark {
        color: #896A28;
      }

      .gray {
        color: var(--gray);
      }

      .handwriting {
        font-family: handwriting;
        letter-spacing: -.2;
      }
{% endstyle %}
