{% comment %}
  Fonts: ATOF faces only.
  Theme JS: preload only global.js (always needed for header/cart wiring).
  Swiper is large (~132 KiB) — let the defer tag discover it without an extra
  early preload competing with LCP on mobile.
  GSAP is interaction-lazy (data-lazy-src) — do not preload.
{% endcomment %}
<link rel="preload" href="{{ 'BrandonGrotesque-Regular.woff2' | asset_url }}" as="font" type="font/woff2" crossorigin>
<link rel="preload" href="{{ 'BrandonGrotesque-Bold.woff2' | asset_url }}" as="font" type="font/woff2" crossorigin>
<link rel="preload" href="{{ 'LouizeDisplayTrial-Medium.woff' | asset_url }}" as="font" type="font/woff" crossorigin>

{{ 'global.js' | asset_url | preload_tag: as: 'script' }}

{% if collection and collection.products.size > 0 %}
  <link rel="preload" as="image"
    href="{{ collection.products.first.featured_image | image_url: width: 800 }}"
    imagesrcset="
      {{ collection.products.first.featured_image | image_url: width: 400 }} 400w,
      {{ collection.products.first.featured_image | image_url: width: 600 }} 600w,
      {{ collection.products.first.featured_image | image_url: width: 800 }} 800w"
    imagesizes="(min-width: 768px) 25vw, 45vw">
{% endif %}
